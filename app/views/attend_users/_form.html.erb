<%= form_for(@attend_user) do |f| %>
  <% if @attend_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attend_user.errors.count, "error") %> prohibited this attend_user from being saved:</h2>

      <ul>
      <% @attend_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.number_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.number_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
