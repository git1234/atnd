<%= form_for(@attend_user) do |f| %>
  <% if @attend_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attend_user.errors.count, "error") %> prohibited this attend_user from being saved:</h2>

      <ul>
      <% @attend_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" style="display:none;">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id, value: current_user.id %>
  </div>
  <div class="field" style="display:none;">
    <%= f.label :event_id %><br>
    <%= f.number_field :event_id, value: @event_id %>
  </div>
  <div class="field" style="display:none;">
    <%= f.label :status %><br>
    <% if AttendUser.find_by(user_id: current_user.id, event_id: @event_id) %>
      <%= f.number_field :status, value:0 %>
    <% else %>
      <%= f.number_field :status, value:1 %>
    <% end -%>
  </div>
    <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="actions">
    <%= f.submit(class: "btn btn-danger") %>
  </div>
<% end %>
